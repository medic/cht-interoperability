import { generateChtRecordsApiUrl } from "./url";

describe("generateChtRecordsApiUrl", () => {
  it("generates a CHT Records API URL when given right chtUrl", () => {

  })
})

describe("generateBasicAuthUrl", () => {
  it("", () => {})
})